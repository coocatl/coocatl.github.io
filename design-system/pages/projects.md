# Projects overrides

- Project order is explicit in data, not encoded through `nth-child` CSS.
- Idle state shows all projects with useful hierarchy.
- Focus state advances one project and retreats neighbors through meaningful direction, overlap, crop, or contrast.
- Cards are editorial image/title units, not rounded containers with generic shadows.
